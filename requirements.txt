sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface (Milestone 5):
gradio>=6.9.0
# streamlit>=1.40.0

# Pin huggingface-hub below 1.0: transformers/sentence-transformers require
# <1.0, while recent gradio prefers >=1.2. The <1.0 side is a hard runtime
# requirement (transformers won't import otherwise); gradio runs fine on 0.36.x
# despite its stricter declared floor. Install this last (or it gets bumped).
huggingface-hub<1.0

# Document ingestion — documents/ contains .pdf files:
pdfplumber==0.11.4
